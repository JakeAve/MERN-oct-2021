// en vez de if / else, se puede guardar un variable en un ternario

const nombre = "Mr. Miyagui";

const esAcceptado = nombre === "Mr. Miyagui" ? "bienvenido" : "quién eres?";

console.table({ esAcceptado });
